# Extract feature spectra

Function to extract the abundance table

## Usage

``` r
extract_feature_spectra(data, rm_low_int = TRUE, min_peaks = 3)
```

## Arguments

- data:

  An MSnExp object in *centroid* mode.

## Value

The return value, if any, from executing the function.

Define a function to remove low intensity peaks
